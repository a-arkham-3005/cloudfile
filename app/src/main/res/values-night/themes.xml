<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Cloudfile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/fg</item>
        <item name="colorControlNormal">@color/fg</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
        <item name="hintTextColor">#999</item>
        <item name="textFillColor">@color/black</item>
        <item name="android:textColorSecondary">@color/fg</item>
        <item name="android:textColorPrimaryDisableOnly">@color/fg</item>
    </style>
    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/fg</item>
    </style>
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/white</item>
        <item name="textAllCaps">false</item>
        <item name="backgroundTint">@color/fg</item>
    </style>
</resources>